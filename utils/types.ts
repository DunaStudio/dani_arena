export interface SectionTextProps {
  label: string;
  title: string;
  text?: string;
  orientation?: "left" | "center";
}

export interface ParaQuienCardProps {
  number: string;
  title: string;
  description: string;
}
